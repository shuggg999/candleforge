# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Conda environments
.conda/

# Docker
*.log
docker-compose.override.yml

# Data (only repo-root /data/ — not src/data/)
/data/
*.db
*.sqlite

# Logs
logs/
*.log

# Config files with secrets
.env
.env.local
.env.production
config/local.yaml
config/secrets.yaml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Test
.pytest_cache/
.coverage
htmlcov/

# Backup and temporary files  
*.bak
*.backup
*.tmp
*.temp
backup/

# ClickHouse local data dir (only root /clickhouse/, NOT config/clickhouse/)
/clickhouse/
*.pid
*.lock

# Runtime data
*.json.log
data_quality_report.json
monitor.log

# OS specific
Thumbs.db
ehthumbs.db

# Claude Code / OpenSpec workspace (user-private)
.claude/settings.local.json
.claude/orchestrator-state.json
# legacy/duplicate schema (Float64 variant, not used by docker-compose); pending cleanup
config/clickhouse/init_freqtrade.sql
# host-specific docker-compose overrides (e.g. production vs local port mappings)
docker-compose.override.yml
docker-compose.override.*.yml

# Pine Script strategies — kept private, not part of the open-source release
pine-strategies/