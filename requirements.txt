# Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# WebSocket and async
websockets==12.0
aiohttp==3.9.1

# Official Exchange connectors (替代 ccxt)
binance-connector==3.12.0
python-okx==0.4.0
pybit==5.6.0

# Database
clickhouse-driver==0.2.6
aiochclient==2.4.0

# Data processing
pandas==2.1.4
numpy==1.26.2
orjson==3.9.10

# Logging
loguru==0.7.2

# Utils
httpx==0.25.2
python-dateutil==2.8.2

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
pytest-cov==4.1.0
black==23.12.0

# Configuration & Monitoring
pyyaml==6.0.1
watchdog==3.0.0  # 用于热重载
prometheus_client==0.19.0

# Scheduling (volume-detection)
apscheduler>=3.10,<4.0

# Test infra (testcontainers for integration tests)
testcontainers[clickhouse]>=4.0,<5.0

# Enhanced retry & utils
tenacity==8.2.3  # 专业重试库
joblib==1.3.2    # 并行处理

# Note: It's recommended to use conda environment from environment.yml
# This requirements.txt is kept as backup for pip-only installations